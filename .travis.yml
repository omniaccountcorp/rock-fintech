language: ruby

rvm:
  - 2.2.5

before_install:
  - cp spec/support/config/config.uat.yml spec/support/config/config.yml

notifications:
  email:
    on_success: always
    on_failure: always

sudo: false

cache: bundler

deploy:
  skip_cleanup: true
  provider: rubygems
  api_key: "YOUR API KEY"
  on:
    tags: true
